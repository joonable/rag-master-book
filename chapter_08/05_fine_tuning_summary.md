# [Chapter 8.5] LLM 파인튜닝 심화 요약: 전략부터 실전까지

본 문서는 RAG 성능을 극대화하기 위한 LLM 파인튜닝의 이론적 배경과 실전 구현 전략, 그리고 하드웨어 및 서비스 선택 가이드를 요약합니다.

---

## 1. RAFT (Retrieval-Augmented Fine-Tuning) 핵심 요약
RAFT는 모델이 **"제공된 문서를 얼마나 영리하게 활용하는가"**를 훈련하는 RAG 특화 파인튜닝 기법입니다.

*   **독약 주입 (Negative Sampling):** 정답 문서(Oracle) 사이에 관련 없는 방해 문서(Distractors)를 섞어, 모델이 노이즈를 걸러내고 진짜 정보를 찾는 '필터링 능력'을 기릅니다.
*   **생각의 사슬 (CoT):** 답변 전 반드시 **[근거 추출] -> [논리적 추론]** 과정을 거치게 하여 할루시네이션을 억제하고 답변의 신뢰도를 높입니다.
*   **오픈 북 시험:** 단순히 지식을 외우는 게 아니라, 주어진 자료에서 답을 도출하는 '추론 근육'을 강화하는 것이 목적입니다.

## 2. 하드웨어 전략: M1 Max vs. A100 SXM
*   **M1 Max (로컬):** 64GB 이상의 통합 메모리를 통해 대형 모델을 로드하고, 데이터 전처리 및 소규모 샘플 학습 루프를 검증하기에 적합합니다. (MLX 라이브러리 활용 권장)
*   **A100 SXM (서버):** 압독적인 연산력(TFLOPS)과 메모리 대역폭을 바탕으로 실제 대규모 데이터셋 학습을 수행합니다. CUDA 에코시스템(bitsandbytes, PEFT 등)을 온전히 활용할 수 있는 표준 환경입니다.

## 3. 파인튜닝 데이터 파이프라인 (03 -> 04)
데이터가 생성(03)되어 학습(04)에 주입되는 과정은 다음과 같습니다.

1.  **생성 (03_xxx.py):** GPT-4o를 비서로 활용하여 원본 문서에서 `instruction(질문)`, `context(정답+방해 문서)`, `cot_answer(추론 정답)` 조각을 추출하고 JSONL로 저장합니다.
2.  **조립 (04_xxx.py):** `formatting_prompts_func`를 통해 흩어진 조각들을 **ChatML 포맷**(`<|im_start|>...`)의 하나의 긴 대화 텍스트로 합칩니다.
3.  **학습 (Training):** 모델은 `user` 메시지까지를 **Input(문제)**으로 인식하고, `assistant` 뒤에 나오는 내용을 **Output(정답)**으로 간주하여 예측 확률을 극대화(Next Token Prediction)합니다.

## 4. 오픈 모델(Qwen/Llama) vs 관리형 서비스(OpenAI/Gemini)

두 방식의 선택 기준은 단순히 '성능'이 아니라 **'데이터 주권, 비용 구조, 엔지니어링 역량'**의 종합적 판단입니다.

### A. 직접 학습 (Open Weights: Qwen, Llama, Mistral)
*   **기술적 제어권 (Full Control):** 
    - 모델의 모든 가중치(Weights)에 접근할 수 있어, 레이어별 튜닝이나 아키텍처 변경이 가능합니다.
    - **LoRA, QLoRA(양자화), PEFT** 등 최신 연구 기법을 자유롭게 적용하여 최적의 효율을 찾을 수 있습니다.
*   **데이터 주권 및 보안 (Privacy):**
    - 데이터를 외부(클라우드 업체)로 보낼 필요 없이 폐쇄망(On-premise)에서 학습과 추론이 가능합니다.
    - 의료, 법률, 군사 등 민감 데이터 취급 시 유일한 대안입니다.
*   **비용 구조 (Cost Model):**
    - **초기 비용:** 고성능 GPU(A100/H100) 대여 또는 구매 비용이 높습니다.
    - **운영 비용:** 학습 후 모델을 직접 서빙(vLLM, TGI 등)하므로, 사용량이 많아질수록 API 대비 토큰당 비용이 기하급수적으로 저렴해집니다.
*   **엔지니어링 부하:** 
    - CUDA 설정, 패키지 의존성 해결, 추론 서버 최적화(Auto-scaling) 등 고도의 DevOps 역량이 필수적입니다.

### B. 관리형 서비스 (Managed API: OpenAI, Google, Anthropic)
*   **간편한 인프라 (Serverless):**
    - GPU 관리나 서버 프로비저닝 없이 데이터 업로드와 버튼 클릭만으로 학습이 완료됩니다.
*   **검증된 '황금 레시피' (Proprietary Recipes):**
    - 제조사가 수만 번의 실험을 통해 찾은 최적의 하이퍼파라미터 세트를 사용하여 학습 실패(Divergence) 확률을 최소화합니다.
*   **추론의 편의성:**
    - 학습 완료 즉시 전용 API 엔드포인트가 생성됩니다. 트래픽 관리는 서비스 제공자가 대행하므로 운영 부담이 없습니다.
*   **비용 구조:** 
    - 학습은 토큰당 비용으로 저렴해 보일 수 있으나, 추론 시 **일반 모델보다 2~3배 비싼 요금**이 청구되므로 대규모 서비스 시에는 부담이 커질 수 있습니다.

## 5. 파인튜닝의 기대 효과 (Business & Technical Impact)

파인튜닝은 모델의 '지식'보다는 **'형식과 기질'**을 바꾸는 데 특효약입니다.

### A. 지시 이행력의 비약적 상승 (Reliability)
- **포맷 강제:** 복잡한 JSON 구조나 특정 코드 스타일을 요구할 때, 일반 모델이 90%의 확률로 지킨다면 파인튜닝 모델은 **99.9% 이상의 신뢰도**를 보여줍니다.
- **예외 처리:** "모르는 질문에는 반드시 '데이터 부족'이라고 답하라"는 식의 가드레일이 모델의 본능으로 각인됩니다.

### B. 프롬프트 압축과 비용 효율화 (Efficiency)
- **Zero-shot 구현:** 일반 모델에게 5~10개의 예시(Few-shot)를 주어야 했던 프롬프트를, 질문 하나로도 원하는 결과를 내게 바꿉니다.
- **토큰 절약:** 시스템 프롬프트 길이를 획기적으로 줄일 수 있어, **입력 토큰 비용 절감 및 응답 대기 시간(Latency) 단축**으로 이어집니다.

### C. 고유한 브랜드 보이스 이식 (Consistency)
- 단순한 어조 설정을 넘어, 특정 기업 고유의 단어 선택, 답변의 길이, 공감의 정도 등을 데이터셋 전체에 걸쳐 균일하게 복제합니다.
- 수천 개의 답변이 마치 **한 명의 전문 상담원**이 작성한 것과 같은 일관성을 가집니다.

### D. 도메인 적응성 (Domain Adaptation)
- **전문 용어 체득:** 일반 모델이 헷갈려 하는 사내 용어, 업계 은어, 약어 등을 문맥에 맞게 정확히 이해하고 구사합니다.
- **문서 구조 이해:** 특정 산업군(예: 법결문, 의무기록) 특유의 논리 전개 방식을 습득하여 더 전문적인 결과물을 산출합니다.

---
**Tip:** 파인튜닝은 '지식 주입'보다 **'행동 양식 교정'**에 더 강력한 도구입니다. 최신 지식은 RAG로 해결하고, 그 지식을 요리하는 방법은 파인튜닝으로 가르치는 것이 베스트 프랙티스입니다.
